{
  "name": "playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist src/types",
    "generate-types": "contentrain generate",
    "predev": "pnpm generate-types"
  },
  "dependencies": {
    "@contentrain/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "rimraf": "^5.0.0"
  }
} 