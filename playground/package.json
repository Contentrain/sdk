{
  "name": "playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "generate:types": "contentrain generate-types --root=. --output=src/types/contentrain.d.ts"
  },
  "dependencies": {
    "@contentrain/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}